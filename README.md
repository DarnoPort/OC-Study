# IGOR — OS Development Study

Репозиторий служит внешней памятью и справочником учебного проекта IGOR, чтобы продолжать обучение в новых чатах без повторного пересказа.

## Проект
- **Название:** IGOR.
- **Цель:** учебная собственная ОС с постепенным освоением принципов ОС.
- **Целевая архитектура:** x86, 32-bit. x86-64 отложена.
- **Michael OS:** отдельный предыдущий проект; использовать как референс, не как целевую ОС.
- **Среда:** Ubuntu доступна и установлена; пользователь подтвердил рабочие GCC, GNU ld и NASM.

## Правила обучения
1. Понимание важнее скорости; идти небольшими шагами.
2. Синтаксис C объяснять с нуля, учитывая основной прежний опыт пользователя в Python.
3. Формат: теория → механика → небольшой пример → мягкая проверка → практика → recap.
4. Не выдавать большие листинги без объяснения назначения.
5. Различать «обсуждено» и «подтверждено ответами/практикой».
6. При затруднениях объяснять спокойно; ошибки локализовать, не переписывать проект без причины.
7. После каждого урока обновлять этот README.

## Учебный прогресс

### Урок 0.1 — ОС и загрузка
**Статус:** базовые идеи частично усвоены; углубление потребуется.
- CPU выполняет машинные инструкции и может работать без ОС.
- ОС — привилегированное системное ПО, управляющее ресурсами и предоставляющее сервисы.
- Упрощённая цепочка: CPU reset → BIOS/UEFI → загрузчик → ядро.
- Загрузчик подготавливает загрузку и передаёт управление ядру.
- C-код напрямую не исполняется CPU: компилятор создаёт машинный код; linker связывает объектные файлы; загрузочный механизм подготавливает запуск.
- Пользователь по существу различил роль CPU, ОС, загрузчика и ядра.

### Урок 0.2 — Инструкции, регистры и память
**Статус:** начальное понимание подтверждено ответами.
- Машинные инструкции, регистры EAX/EBX/ECX/EDX/ESP/EBP, `mov`, `add`.
- EIP содержит адрес следующей инструкции; переходы могут изменить поток выполнения.
- RAM моделируется как адрес → содержимое. `mov eax, 0x2000` загружает число/адрес, а `mov eax, [0x2000]` читает память по адресу.
- `0x` — шестнадцатеричная запись; скобки `[]` в Assembly означают обращение к памяти.
- Пользователь правильно решил примеры с регистрами, адресами RAM и различием immediate vs memory operand.

### Уроки 0.2.1–0.2.2 — Основы C и указатели
**Статус:** базовое понимание подтверждено; закреплять по мере практики.
- `int x = 123;`: тип, имя, присваивание, значение; тип влияет на интерпретацию/размер объекта.
- `&x` получает адрес; `int *p` — указатель; `p` хранит адрес, `*p` обращается к объекту по адресу.
- `*p = 100` записывает значение в исходный объект через указатель.
- `int p = x` копирует значение, а `int *p = &x` хранит адрес.
- Пользователь верно определил значения в задачах с указателями. Ранее путал изменение указателя с изменением объекта; отдельно закреплено: `p = &y` меняет адрес в p, но само x не изменяет.

### Урок 0.3 — Функции, стек, `call` и `ret`
**Статус:** базовое понимание подтверждено ответами; подробности calling convention отложены.
- Разобраны функции C, параметры, `return`, стек как LIFO, ESP как адрес вершины стека, EBP как ориентир stack frame в упрощённой модели.
- `push` уменьшает ESP на 4 байта в 32-битном примере; `pop` извлекает верхнее значение.
- `call` сохраняет адрес возврата в стеке и передаёт выполнение функции; `ret` извлекает адрес возврата и загружает его в EIP.
- Пользователь верно объяснил LIFO аналогией с тарелками, правильно определил результаты записи через указатель и ESP после push/call.
- Уточнено: при `call` EIP переходит на функцию; адрес инструкции после call сохраняется для возврата.

### Урок 0.4 — C → 32-bit x86 → ELF → linker
**Статус:** завершён; практическое понимание подтверждено.
- Проверено реальное окружение Ubuntu:
  - GCC 15.2.0
  - GNU ld 2.46
  - NASM 3.01
- Написана и запущена обычная C-программа `hello.c`; увидена разница между C-кодом и машинным кодом.
- Через `gcc -S` просмотрен Assembly обычной 64-битной Linux-программы: `RAX/RSP/RBP`, вызов `printf`, передача аргумента и возврат.
- Через `gcc -m32 -S` просмотрен 32-битный x86 Assembly; пользователь связал `EAX/ESP/EBP` с выбранной архитектурой.
- Обсуждено, что 64-битное x86 — расширение 32-битного x86: `RAX/RSP/RBP` шире соответствующих `EAX/ESP/EBP`.
- Создан freestanding C-код без стандартной библиотеки:
  `volatile int x = 42;` и бесконечный цикл.
- Выполнено `gcc -m32 -ffreestanding -c`; получен `ELF 32-bit LSB relocatable, Intel i386`.
- Через `objdump -d` пользователь увидел реальные инструкции `push`, `mov`, `sub`, `call`, `ret`, `jmp` и связал `volatile int x = 42` с машинной инструкцией записи `0x2a`.
- Выполнено `ld -m elf_i386`; получен `ELF 32-bit executable`, статически связанный ELF-файл.
- Закреплено различие: compiler переводит исходный C в объектный код; linker собирает объектный код в исполняемый ELF и разрешает размещение/связи.
- Пользователь правильно понял, что аргумент функции не обязан лежать в стеке: способ передачи зависит от ABI. Для рассмотренного 64-битного System V ABI первый аргумент передаётся через `RDI`; детали 32-битного calling convention пока не закреплены.
- Ограничение текущей практики: полученный ELF всё ещё рассчитан на обычную Linux-среду и не является ядром IGOR; служебные детали PIC/GOT в GCC-коде сознательно отложены.

## Урок 0.5 — каркас IGOR: entry point → загрузка → C
**Статус:** начат; практическая часть ещё не выполнена.
- Цель: понять, почему собственному ядру нужен явный entry point и почему `main()` не является магической точкой старта процессора.
- Следующий практический шаг: минимальный 32-битный Assembly entry point, отдельный C-код ядра и linker script; затем сборка bootable-образа через выбранный загрузочный механизм.
- Не считать выполненным, пока пользователь не соберёт и не проверит результат.

## Текущая точка остановки
Урок 0.4 завершён. Пользователь уже видел полный подготовительный путь от C до 32-битного ELF executable и понимает базовую роль compiler и linker. Calling convention и служебные детали ELF/PIC/GOT пока не считаются полностью освоенными.

**Текущий урок:** 0.5 — первый настоящий каркас IGOR. Начинаем с понимания entry point и отделения стартового Assembly от C-кода. После этого постепенно перейдём к загрузке/bootable-образу.

## Предварительная дорожная карта
1. Фундамент: CPU, RAM, машинный код, регистры, стек.
2. Инструментарий: C в freestanding-среде, x86 Assembly, compiler/linker, object/ELF.
3. Загрузка: BIOS/UEFI в выбранной конфигурации, bootloader, передача управления.
4. Минимальное ядро IGOR: entry point, linker script, отладочный вывод, сборка/эмуляция.
5. Прерывания и исключения: IDT, IRQ, обработчики.
6. Память: физическая/виртуальная, стек, куча, аллокатор.
7. Устройства и I/O: таймер, клавиатура, диск, драйверная модель.
8. Файловая система.
9. Пользовательские программы, ELF, системные вызовы, user mode.
10. Процессы и планирование.
11. Оболочка и инструменты.
12. Надёжность, тестирование, права, расширяемость; позже — x86-64.

## Карточка навыков
| Тема | Состояние | Примечание |
|---|---|---|
| Зачем нужна ОС; CPU без ОС | Начальное понимание | Урок 0.1 |
| Загрузчик vs ядро | Начальное понимание | Требуется углубление |
| Компилятор | Практически подтверждено | Урок 0.4: C → Assembly/object |
| Linker | Практически подтверждено | Урок 0.4: .o → ELF executable |
| ELF object/executable | Начальное практическое понимание | Увидены file/objdump |
| x86 32-bit | Цель выбрана и практически подтверждена | Получен ELF i386 |
| Регистры, `mov`/`add` | Начальное понимание подтверждено | Простые задачи + реальный Assembly |
| EIP | Начальное понимание подтверждено | Связан с call/ret |
| RAM, адреса и `[]` | Начальное понимание подтверждено | Адрес и содержимое различает |
| Базовый синтаксис C | Начальное понимание подтверждено | Новые конструкции объяснять с нуля |
| Указатели | Начальное понимание подтверждено | Различие p и *p; дальше закреплять практикой |
| Стек, ESP, `call`/`ret` | Начальное понимание подтверждено | Упрощённая модель; углубление позже |
| Calling convention | Обсуждено, не закреплено | Первый аргумент 64-bit ABI увиден на практике |
| Ubuntu toolchain | Практически подтверждено | GCC 15.2, ld 2.46, NASM 3.01 |
| Freestanding C | Практически подтверждено | `-ffreestanding`, объектный ELF |
| Bootable kernel | Не начато | Урок 0.5 |

## Протокол обновления README
После каждого урока фиксировать пройденные темы, подтверждённое понимание, затруднения, текущую точку остановки, следующий шаг и технические решения (отделяя подтверждённое от предварительного). Не выдумывать выполненную работу или характеристики.
